name: build-linux-targeted

on:
  push:
    branches: ["**"]
  pull_request:
  workflow_dispatch:

jobs:
  build-centos7-skylake:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          submodules: recursive

      # 1. Create the Build Script (Run inside Docker)
      - name: Create Build Script
        run: |
          cat << 'EOF' > build_internal.sh
          #!/bin/bash
          set -eo pipefail 

          # --- FIX REPOS (Minimal fix just to get bzip2/wget) ---
          rm -f /etc/yum.repos.d/*.repo
          cat << 'REPO' > /etc/yum.repos.d/CentOS-Vault.repo
          [base]
          name=CentOS-7 - Base
          baseurl=http://vault.centos.org/7.9.2009/os/x86_64/
          gpgcheck=0
          
          [updates]
          name=CentOS-7 - Updates
          baseurl=http://vault.centos.org/7.9.2009/updates/x86_64/
          gpgcheck=0
          REPO
          
          yum install -y bzip2 wget

          # --- INSTALL MICROMAMBA & GCC 13 ---
          wget -qO- https://micro.mamba.pm/api/micromamba/linux-64/latest | tar -xj -C /usr/local/bin --strip-components=1 bin/micromamba
          
          export MAMBA_ROOT_PREFIX=/opt/conda
          micromamba create -y -n build-env -c conda-forge \
            gcc_linux-64=13 gxx_linux-64=13 \
            cmake ninja pkg-config make \
            ragel patchelf \
            brotli zlib lz4-c zstd snappy xz bzip2 \
            liblzma-devel

          # --- ACTIVATE ENVIRONMENT ---
          set +u
          eval "$(micromamba shell hook -s bash)"
          micromamba activate build-env
          set -u

          # --- CONFIGURE COMPILER PATHS ---
          ENV_PATH="/opt/conda/envs/build-env"
          
          export CFLAGS="-I$ENV_PATH/include $CFLAGS"
          export CXXFLAGS="-I$ENV_PATH/include $CXXFLAGS"
          export LDFLAGS="-L$ENV_PATH/lib -Wl,-rpath,$ENV_PATH/lib $LDFLAGS"
          
          # --- CRITICAL FIX: MANUALLY CREATE SNAPPY.PC ---
          # Conda's snappy package often misses this file, causing FindPkgConfig to fail.
          mkdir -p $ENV_PATH/lib/pkgconfig
          cat << PC > $ENV_PATH/lib/pkgconfig/snappy.pc
          prefix=$ENV_PATH
          exec_prefix=\${prefix}
          libdir=\${prefix}/lib
          includedir=\${prefix}/include

          Name: snappy
          Description: Snappy compression library
          Version: 1.1.10
          Libs: -L\${libdir} -lsnappy
          Cflags: -I\${includedir}
          PC
          
          export PKG_CONFIG_PATH="$ENV_PATH/lib/pkgconfig:$PKG_CONFIG_PATH"

          # --- ENVIRONMENT SETUP ---
          export SQUASH_PREFIX=/usr/local
          export TARGET_ARCH="-march=skylake-avx512 -mtune=skylake-avx512"
          
          echo "Compiler Version Check:"
          $CXX --version

          # --- DOWNLOAD & BUILD SQUASH 0.7.0 ---
          wget https://github.com/quixdb/squash/releases/download/v0.7.0/squash-0.7.0.tar.bz2
          tar -xjf squash-0.7.0.tar.bz2
          
          cd squash-0.7.0
          
          # Configure Squash
          cmake -B build -G Ninja \
            -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_INSTALL_PREFIX=${SQUASH_PREFIX} \
            -DCMAKE_C_FLAGS="${TARGET_ARCH} ${CFLAGS} -w" \
            -DCMAKE_CXX_FLAGS="${TARGET_ARCH} ${CXXFLAGS} -w" \
            -DCMAKE_POLICY_VERSION_MINIMUM=3.5
            
          cmake --build build -j
          make -C build install || cmake --install build
          
          # Copy libs to conda env so bundle picks them up
          cp -v ${SQUASH_PREFIX}/lib/libsquash* $ENV_PATH/lib/ || true
          
          cd /workspace

          # --- BUILD YOUR PROJECT ---
          cmake -S . -B build-skylake -G Ninja \
            -DCMAKE_BUILD_TYPE=Release \
            -DARCH_FLAGS="${TARGET_ARCH}" \
            -DNEATS_WITH_SQUASH=ON

          cmake --build build-skylake -j

          # --- BUNDLE ---
      - name: Bundle runtime
        run: |
          set -euxo pipefail

          BIN="build-skylake/LosslessBenchmarkFull"
          OUT="NeaTS-CentOS7-Skylake"
          
          rm -rf "$OUT"
          mkdir -p "$OUT/lib/squash"
          
          cp -v "$BIN" "$OUT/LosslessBenchmarkFull"
          
          # Fix RPATH to look in $ORIGIN/lib
          patchelf --set-rpath '$ORIGIN/lib' "$OUT/LosslessBenchmarkFull"

          # 1. COPY BINARY DEPENDENCIES (e.g., liblz4.so, libstdc++.so)
          # We copy from the Micromamba environment explicitly
          ldd "$OUT/LosslessBenchmarkFull" | awk '{print $3}' | grep -E '^/' | sort -u > /tmp/libs.txt || true
          while read -r lib; do
            base="$(basename "$lib")"
            case "$base" in
              ld-linux*|libc.so.*|libm.so.*|libpthread.so.*|librt.so.*|libdl.so.*)
                continue ;;
            esac
            # Force copy from the build environment path to ensure we get the newer versions
            if [ -f "/opt/conda/envs/build-env/lib/$base" ]; then
                cp -vL "/opt/conda/envs/build-env/lib/$base" "$OUT/lib/"
            else
                cp -vL "$lib" "$OUT/lib/" || true
            fi
          done < /tmp/libs.txt

          # 2. COPY SQUASH PLUGINS
          # We check both lib and lib64 to be safe
          if [ -d "/usr/local/lib/squash" ]; then
             cp -r /usr/local/lib/squash/* "$OUT/lib/squash/" || true
          fi
          if [ -d "/usr/local/lib64/squash" ]; then
             cp -r /usr/local/lib64/squash/* "$OUT/lib/squash/" || true
          fi

          # 3. GENERATE ROBUST RUN SCRIPT
          cat << 'EOF' > "$OUT/run.sh"
          #!/bin/bash
          
          # Get absolute path to the bundle directory
          HERE="$(dirname "$(readlink -f "$0")")"
          
          # 1. Set library path so plugins can find liblz4.so, libzstd.so, etc.
          export LD_LIBRARY_PATH="$HERE/lib:$LD_LIBRARY_PATH"
          
          # 2. Auto-detect the Squash Plugins directory
          # Squash 0.7 usually nests plugins deep, e.g., lib/squash/0.7/plugins
          # We find the folder named 'plugins' and point SQUASH_PLUGINS_PATH to it.
          PLUGIN_DIR=$(find "$HERE/lib/squash" -type d -name "plugins" | head -n 1)
          
          if [ -n "$PLUGIN_DIR" ]; then
              export SQUASH_PLUGINS_PATH="$PLUGIN_DIR"
              # Also set the generic path just in case
              export SQUASH_PATH="$(dirname "$PLUGIN_DIR")"
          else
              echo "WARNING: Could not find Squash 'plugins' directory in $HERE/lib/squash"
          fi
          
          # 3. Run the benchmark
          exec "$HERE/LosslessBenchmarkFull" "$@"
          EOF
          
          chmod +x "$OUT/run.sh"
          
          # Fix ownership for artifact upload
          chown -R $(id -u):$(id -g) "$OUT"
          EOF
          
          chmod +x build_internal.sh

      # 2. Run Build in Docker
      - name: Run Build in Docker (CentOS 7)
        run: |
          docker run --rm \
            -v ${{ github.workspace }}:/workspace \
            -w /workspace \
            quay.io/pypa/manylinux2014_x86_64:latest \
            /bin/bash /workspace/build_internal.sh

      # 3. Upload Artifact
      - name: Upload Bundle
        uses: actions/upload-artifact@v4
        with:
          name: NeaTS-CentOS7-Skylake
          path: NeaTS-CentOS7-Skylake
          compression-level: 9