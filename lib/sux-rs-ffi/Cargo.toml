[package]
name = "sux-rs-ffi"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["staticlib"]

[dependencies]
sux = { git = "https://github.com/vigna/sux-rs.git", branch = "main" }
mem_dbg = "0.4"
value-traits = "0.2"

[profile.release]
lto = true
codegen-units = 1
